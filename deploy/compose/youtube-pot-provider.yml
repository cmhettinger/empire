services:
  youtube-pot-provider:
    image: ${EMPIRE_YOUTUBE_POT_PROVIDER_IMAGE:-brainicism/bgutil-ytdlp-pot-provider:1.3.1}
    container_name: ${EMPIRE_YOUTUBE_POT_PROVIDER_CONTAINER:-empire-youtube-pot-provider}
    restart: unless-stopped

    networks:
      - empire-net
