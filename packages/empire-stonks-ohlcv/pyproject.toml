[tool.poetry]
name = "empire-stonks-ohlcv"
version = "0.1.0"
description = "Reusable provider-native OHLCV ingestion utilities for Empire stonks."
readme = "README.md"
authors = ["Empire"]
packages = [{ include = "empire_stonks_ohlcv", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
empire-core = { path = "../empire-core" }
empire-reports = { path = "../empire-reports" }
pandas-market-calendars = ">=5.4,<6"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[tool.poetry.scripts]
stonks-ohlcv-config = "empire_stonks_ohlcv.scripts.config:main"
stonks-ohlcv-eoddata-daily = "empire_stonks_ohlcv.scripts.eoddata_daily:main"
stonks-ohlcv-stooq-backfill = "empire_stonks_ohlcv.scripts.stooq_backfill:main"
stonks-ohlcv-yahoo-backfill = "empire_stonks_ohlcv.scripts.yahoo_backfill:main"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "../empire-core/src", "../empire-reports/src"]
