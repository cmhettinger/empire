[tool.poetry]
name = "empire-stonks-securities"
version = "0.1.0"
description = "Reusable securities reference-data utilities for Empire stonks."
readme = "README.md"
authors = ["Empire"]
packages = [{ include = "empire_stonks_securities", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
empire-core = { path = "../empire-core" }
PyYAML = ">=6.0"
requests = ">=2.31"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "../empire-core/src"]
